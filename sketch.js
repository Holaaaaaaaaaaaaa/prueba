function setup() {
  createCanvas(600, 400);
  //canvas.parent("lienzo");
  background(220);
}

function draw() {
  fill(random (0),random (255),random (216));
  textSize(40);
  text("AAAAAA",mouseX,mouseY);
}